export enum MatchEvents {
  INVITATION = 'match:invitation',
  NEW_PLAYER_TO_MATCH = 'match:new_player_to_match',
  CANCELLED = 'match:cancelled',
  PLAYER_LEFT_MATCH = 'match:player_left_match',
  MATCH_READY = 'match:match_ready',
}
